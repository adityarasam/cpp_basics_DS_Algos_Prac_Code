#include "RecurDynamic.h"



int main()
{
   

   
    return 0;
}


/*

cout<< CountWaysDyn(6) << endl;

ROBOT
vector<pair<int, int>> myPath;
vector<vector<bool>> myMaze(5, vector<bool>(5));



myMaze = createGirdMaze(5, false);
myPath = getPath(myMaze);
    

for(auto const& v: myPath)
{
    cout<< "(" << v.first <<","<< v.second << ")"<< endl;
}


return 0;



//----------------MAGIC NUMBER-----------------------
 array.emplace_back(-5);
    array.emplace_back(-2);
    array.emplace_back(-1);
    array.emplace_back(0);
    array.emplace_back(1);
    array.emplace_back(5);
    array.emplace_back(7);
    array.emplace_back(9);
    array.emplace_back(10);

    x=findMagicFast(array);
    cout<< "Num is: "<< x<< endl; 



vector<int> array;
int x=0;

*/
